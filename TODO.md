# 待办事项清单

## 当前进度

- 已完成事项已从清单中移除。
- 待执行：1 ～ 35

## 优先级评估原则

- P0-A：公网内测上线前必须完成，目标是让当前 V0.1 MVP 安全、可用、可观测地跑起来。
- P1：M2 MVP 闭环必须完成，目标是验证“用户会围绕同一个关系档案反复保存与更新分析”。
- P2：提升 M2 内测体验、稳定性、测试覆盖和数据观测。
- P3：内测后增强项，不阻塞 M2 MVP 验证。

## 内测阶段待办

### P0-A：公网内测上线前必须完成

1. `[待执行]` 确认公网域名、DNS 与安全组

目标服务器：`43.99.26.109`，SSH 用户：`root`。确认内测域名或子域名，将 A 记录指向目标服务器；在云厂商安全组开放 `22/tcp`、`80/tcp`、`443/tcp`，不开放 PostgreSQL、Prometheus、Grafana 或后端服务端口。

1. `[待执行]` 准备空服务器基础环境

在服务器安装 Docker、Docker Compose 插件、Git、必要系统依赖和基础防火墙规则。确认 `docker compose version` 可用，服务器时间同步正常，项目部署目录和权限规划清晰。

1. `[待执行]` 将项目代码部署到服务器

根据仓库可访问性选择 `git clone`、Deploy Key、GitHub token 或本地打包上传。部署目录不保存明文凭据；后续更新优先通过可重复的拉取 / 构建流程完成。

1. `[待执行]` 配置生产 `.env` 与最小权限

在服务器项目目录创建 `.env`，填写数据库密码、Kimi Key、CORS 域名、邀请码、认证密钥、Grafana 管理员密码等生产配置。`.env` 不写入仓库，不输出到日志，设置最小文件权限，例如仅 root 可读写。

1. `[待执行]` 调整公网反向代理暴露面

后端容器不直接暴露到公网，只通过 Nginx 访问；PostgreSQL 仅绑定本机或 Docker 内网；Prometheus、Grafana 仅允许通过 SSH 隧道或服务器本机访问。公网 Nginx 不暴露 `/metrics`。

1. `[待执行]` 配置 HTTPS 证书与域名访问

使用 Let's Encrypt、Caddy、宿主机 Nginx 或其他可靠方式为内测域名配置 HTTPS。首次部署优先选择 Caddy：配置简单，可自动申请并续期 Let's Encrypt 免费证书；如后续需要更细的 Nginx 控制，再选择宿主机 Nginx + Certbot，并用 `certbot renew --dry-run` 验证自动续期。HTTP 访问应跳转到 HTTPS；证书自动续期可用；确认前端页面、API 代理和上传大小限制正常。

1. `[待执行]` 启动 Docker Compose 生产服务

在服务器执行构建和启动，确认 `web`、`backend`、`postgres`、`prometheus`、`grafana`、`cadvisor` 容器状态正常。重启后数据卷可保留数据库和上传文件。

1. `[待执行]` 完成公网冒烟测试

确认 `https://内测域名/health` 返回正常；注册 / 登录、文本分析、截图分析、保存分析、历史记录、删除记录均可用。验证未登录请求被拒绝、限流生效、超时提示友好。

1. `[待执行]` 完成基础安全检查与上线记录

检查公网端口暴露、容器端口映射、`.env` 权限、日志敏感信息、Nginx 访问路径、Docker volume 持久化和服务器重启恢复。记录部署命令、回滚方式、证书续期方式和常用排查命令。

### P1：M2 MVP 留存闭环必须完成

1. `[待执行]` 明确 M2 报告契约与指标口径

确认阶段枚举、中文标签、置信度字段、IVI / SPE / EWS 指标定义、风险类型、风险等级、`blocked` 规则、行动卡字段和免责声明字段。该契约应作为后端响应、数据库保存、前端渲染和测试断言的共同依据。

1. `[待执行]` 设计并迁移 M2 数据模型

新增或调整关系档案、分析与档案绑定、结构化消息、行动标签、反馈记录、支付状态等表结构。保证反馈必须关联用户和分析记录，若分析已绑定档案则同时关联档案。

1. `[待执行]` 升级后端分析记录保存格式

保存确认后的结构化消息、稳定报告结构、关键证据、风险拦截结果、行动卡和低置信度标记。历史详情接口需要兼容旧记录，并能返回 M2 页面所需字段。

1. `[待执行]` 支持长聊天文本导入并解析为结构化消息

用户可以粘贴长聊天记录；系统解析为消息列表，并尽量识别说话人、时间或分段。解析失败时仍要允许用户手动修正。

1. `[待执行]` 支持 1 至 9 张截图按顺序导入

截图上传应保留用户选择顺序；OCR 结果需要合并为可编辑的结构化消息列表。需要处理 OCR 失败、部分图片失败、顺序异常和超出数量限制的提示。

1. `[待执行]` 增加分析前会话确认与编辑界面

用户在提交分析前可以编辑消息文本，并修改说话人身份为“自己 / 对方”。只有确认后的结构化消息才能进入分析流程。

1. `[待执行]` 升级分析报告生成逻辑

每次分析返回稳定一致的报告结构：阶段、态度摘要、IVI / SPE / EWS、至少三条证据、推荐回复、备选回复、低置信度标记、行动卡、风险字段和免责声明。

1. `[待执行]` 强化风险拦截与安全回复策略

对黄暴相关信息和其他高风险场景设置风险等级、风险类型、风险说明和 `blocked` 标记。被拦截时不得生成不安全推荐回复，目标是风险拦截回复安全性达到 100%。

1. `[待执行]` 实现关系档案创建、列表与编辑

档案字段包括昵称、渠道、当前阶段、当前风险等级、当前动作标签、距离上次分析之间的实际动作标签、创建时间和更新时间。用户输入字段与分析输入字段要区分来源。

1. `[待执行]` 实现分析保存与档案绑定流程

一个分析结果可以不绑定，也可以精确绑定到一个档案。需要确定主流程入口：优先评估“先建档再分析且默认绑定”与“先分析再无感绑定”两种方案，并落地一种默认路径。

1. `[待执行]` 实现档案详情洞察页

档案详情页展示最新阶段、最新动作卡、最新风险状态、阶段趋势、最近 5 次分析的 IVI / SPE / EWS 简化趋势，以及已绑定分析的时间线。

1. `[待执行]` 实现档案时间线记录

时间线展示每次分析的摘要、关键证据、阶段、风险和反馈状态，支持进入对应分析详情。

1. `[待执行]` 支持从档案直接发起新分析

档案详情页提供新分析入口，跳转后默认携带并绑定当前档案，减少用户重复选择成本。

1. `[待执行]` 支持用户反馈“准 / 不准”

用户可以对每次分析标记“准确 / 不准确”，并可选填写原因说明。反馈数据用于后续优化规则、提示词和报告可信度。

1. `[待执行]` 增加基础支付系统

实现 M2 所需的基础支付闭环，包括支付状态记录、订单或订阅记录、支付成功回调处理、权限校验和前端状态展示。若暂不接真实支付渠道，至少保留可替换的接口与数据模型。

支付宝官方直连细项（参考：https://opendocs.alipay.com/support/01raxx?ant_source=opendoc_recommend）：

- `[待执行]` 确认收款主体与支付宝商家账号  
  确认使用个人、个体工商户还是企业主体；完成支付宝实名认证、商家信息补全、经营类目、结算银行卡和收款账号准备。明确是否具备开通 `当面付` 所需资质。

- `[待执行]` 创建支付宝开放平台网页 / 移动应用  
  登录支付宝开放平台创建应用，记录 `APPID`、应用名称、应用类型、开发者账号和应用状态。确认应用绑定到正确商家主体。

- `[待执行]` 申请并绑定当面付产品权限  
  申请 `当面付`，用于生成动态扫码支付二维码；检查产品是否签约成功，避免接口调用时出现无权限或未签约错误。

- `[待执行]` 配置 RSA2 密钥与验签材料  
  使用支付宝开放平台开发助手或 OpenSSL 生成应用私钥和应用公钥；将应用公钥上传至开放平台；保存支付宝公钥用于验签。应用私钥、支付宝公钥、`APPID` 等只写入服务器 `.env` 或密钥管理，不提交仓库。

- `[待执行]` 配置支付宝网关与回调地址  
  区分沙箱网关和正式网关；配置异步通知地址，例如 `https://内测域名/api/payments/alipay/notify`。回调地址必须公网 HTTPS 可访问。

- `[待执行]` 设计支付宝订单与额度数据模型  
  增加支付订单表和用户额度表或用户权益字段。订单至少记录 `out_trade_no`、用户、套餐、金额、币种、状态、支付宝交易号、通知原文、创建时间和支付时间。

- `[待执行]` 实现创建支付宝支付订单接口  
  后端创建本地 `pending` 订单，调用 `alipay.trade.precreate` 生成动态二维码；返回二维码链接、订单号和轮询信息给前端。首版商品建议使用一次性分析额度包。

- `[待执行]` 实现前端扫码支付页面  
  前端展示支付宝二维码、金额、套餐内容和订单状态；轮询本地订单状态接口；支付成功后展示额度到账状态。

- `[待执行]` 实现本地订单状态查询接口  
  前端可以查询当前用户自己的订单支付状态和到账状态；接口必须校验登录态与订单归属，避免跨用户查看订单。

- `[待执行]` 实现支付宝异步通知 webhook  
  接收支付宝 `notify` 请求，完成验签、订单号校验、金额校验、商户 / 应用校验和交易状态校验。仅在 `TRADE_SUCCESS` 或 `TRADE_FINISHED` 时将订单置为已支付。

- `[待执行]` 实现支付成功后的幂等发放权益  
  webhook 成功后为用户增加分析额度或开通会员权益；同一订单重复通知不得重复发放。处理完成后按支付宝要求返回纯文本 `success`。

- `[待执行]` 完成支付宝沙箱联调  
  使用沙箱 `APPID`、沙箱网关、沙箱密钥和沙箱买家账号完成 0.01 元或测试金额支付；验证二维码生成、支付成功通知、重复通知、订单状态轮询和额度到账。

- `[待执行]` 完成正式环境小额验收  
  切换正式 `APPID`、网关和密钥后，用小额真实订单验证全链路。检查正式域名 HTTPS、回调可达性、日志脱敏、订单对账和异常告警。

### P2：M2 内测体验与稳定性

1. `[待执行]` 增加邮件异常监控告警

对 5xx 错误、接口超时、Kimi 超时 / 失败、OCR 失败率异常升高、数据库连接异常、限流异常峰值等情况配置邮件告警。告警邮件需要包含环境、接口路径、请求 ID、错误类型、时间窗口和简短排查入口，避免泄漏敏感数据，并设置合理阈值、静默窗口和去重策略，避免内测期间被噪声淹没。

1. `[待执行]` 补充后端单元测试

覆盖热情样例、冷淡样例、风险 / 边界样例、阶段识别、参数量化、策略决策、结构化报告生成、档案绑定和超时分支，避免后续 AI 相关代码调整影响已有核心判断。

1. `[待执行]` 补充 API 集成测试

覆盖注册、登录、邀请码校验、登录态、用户隔离、历史列表、历史详情、保存分析、删除记录、未授权访问、跨用户访问拦截、关系档案、反馈和支付状态等主流程。

1. `[待执行]` 补充 M2 端到端冒烟测试

覆盖文本导入、截图导入、消息编辑确认、生成报告、保存或绑定档案、档案详情趋势、时间线、从档案发起二次分析、提交反馈和支付状态展示。

1. `[待执行]` 统计 M2 成功标准相关指标

记录首次分析完成率、分析保存 / 绑定率、同一档案二次分析率、风险拦截命中与安全性、总访问次数、登录用户数、活跃用户数等指标。

1. `[待执行]` 统计分析行为与失败情况

记录分析次数、保存次数、删除次数、失败次数、平均耗时、Kimi 超时率、OCR 失败率、结构化解析失败率、反馈准确率等指标，帮助定位体验瓶颈和成本压力。

1. `[待执行]` 优化 OCR 质量

支持长图切片、图片预处理、低置信度提示等能力，提高截图分析成功率。

### P3：内测后迭代

1. `[待执行]` 支持历史记录重命名

用户可以给已保存的分析添加标题或备注，方便回看和区分不同聊天对象 / 场景。

1. `[待执行]` 增加管理后台或简易运营视图

提供内测期间查看用户数、请求量、失败率、Kimi 超时率、OCR 失败率、结构化解析失败率、用户反馈统计和支付状态统计的轻量入口。

1. `[待执行]` 优化档案洞察可视化

在 M2 基础趋势可用后，再评估是否接入图表库、增加阶段变化解释、行动效果复盘和更细的时间范围筛选。

1. `[待执行]` 优化档案无感绑定体验

基于内测数据验证默认建档、分析后推荐绑定、自动识别同一关系对象等方案，降低用户维护档案的心理成本。
